import { Component } from '@angular/core';
/**
 * DashboardComponent is the main component for the dashboard page.
 * It is responsible for displaying the dashboard layout and managing the dashboard data.
 * It uses the DashboardService to fetch and manage the dashboard data.
 */
@Component({
  selector: 'app-dashboard',
  templateUrl: './dashboard.component.html',
  styleUrls: ['./dashboard.component.scss']
})
export class DashboardComponent {}
